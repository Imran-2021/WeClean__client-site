import React from 'react';

const UserDetails = (props) => {
    return (
        <div>          
                   <p>{props.data}</p>
                   <p>{props.dataa}</p>
                   <p>{props.dataaa}</p>
        </div>
    );
};

export default UserDetails;